import React from "react"
import PropTypes from "prop-types"
class TrackListing extends React.Component {
  render () {
    return (
      <React.Fragment>
        In the track listing component 
      </React.Fragment>
    );
  }
}

export default TrackListing
